<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name="com.vanniktech.emoji.sample.EmojiApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@null"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning, UnusedAttribute"
        tools:targetApi="s">
        <activity
            android:name=".MainActivityEmojiEditText"
            android:exported="true"
            android:windowSoftInputMode="stateVisible|adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CustomViewActivity"
            android:windowSoftInputMode="stateVisible|adjustResize" />
        <!--
        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="stateVisible|adjustResize" />
        <activity
            android:name=".MainActivityAutoCompeteTextView"
            android:windowSoftInputMode="stateVisible|adjustResize" />
        <activity
            android:name=".MainActivityMultiAutoCompeteTextView"
            android:windowSoftInputMode="stateVisible|adjustResize" />
        -->
    </application>
</manifest>
